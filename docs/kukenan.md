# Kukenan

This is my `neovim` configuration using neovim and `primamateria` implementation

## Flavors

This configurations contain a `base` configuration and other flavors that use this as a base.
The other flavors are `web`, `java`, `rust`, `csharp`.
